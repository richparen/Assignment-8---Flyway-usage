CREATE TABLE Users(
                      id int,
                      name varchar,
                      password varchar,
                      subscribe varchar,
                      type varchar
);

create table music(
    id int,
    name varchar,
    yearOfPublish int,
    min varchar
);